% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggNetView_RMT2.R
\name{ggNetView_RMT2}
\alias{ggNetView_RMT2}
\title{ggNetView_RMT2: 一次性、非交互的随机矩阵阈值扫描与自动选阈值}
\usage{
ggNetView_RMT2(
  mat,
  nr_thresholds = 51,
  interval = NULL,
  unfold.method = c("gaussian", "spline"),
  bandwidth = "nrd0",
  nr.fit.points = 51,
  discard.outliers = TRUE,
  discard.zeros = TRUE,
  min.mat.dim = 40,
  max.ev.spacing = 3,
  save_plots = FALSE,
  out_dir = "RMT_plots",
  verbose = TRUE
)
}
\arguments{
\item{mat}{数值型、实对称矩阵（如相关矩阵、邻接矩阵）}

\item{nr_thresholds}{整数。阈值扫描的格点数，默认 51}

\item{interval}{长度为 2 的数值向量。阈值搜索区间（基于 |mat| 的上三角），默认自动[min, max]}

\item{unfold.method}{"gaussian" 或 "spline"，默认 "gaussian"}

\item{bandwidth}{仅对 gaussian 展开有效。传递给 density() 的 bw，默认 "nrd0"}

\item{nr.fit.points}{仅对 spline 展开有效。样条拟合采样点数，默认 51}

\item{discard.outliers}{是否在展开前剔除特征值异常点（IQR：1.5），默认 TRUE}

\item{discard.zeros}{在每个阈值下是否丢弃全零的行列（保持对角），默认 TRUE}

\item{min.mat.dim}{有效矩阵维度（非零行列）低于此值提前停止扫描，默认 40}

\item{max.ev.spacing}{计算 NNSD 时的最大间距截断（去除极大 spacing 的长尾），默认 3}

\item{save_plots}{是否保存诊断图，默认 FALSE（非交互；保存为 png）}

\item{out_dir}{图像输出目录（当 save_plots=TRUE 时必须有写权限），默认 "RMT_plots"}

\item{verbose}{是否打印简要进度信息，默认 TRUE}
}
\description{
ggNetView_RMT2: 一次性、非交互的随机矩阵阈值扫描与自动选阈值
}
\examples{
NULL
# m <- cor(scale(matrix(rnorm(40000), 200, 200)))
# res <- ggNetView_RMT(m, save_plots=TRUE)
# res$chosen_threshold

}
